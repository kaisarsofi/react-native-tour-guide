# Strip tool co-author trailers Cursor/Claude inject into commit messages.
MSG_FILE="$1"
[ -f "$MSG_FILE" ] || exit 0

tmp="$(mktemp)"
grep -E -v -i '^Co-authored-by:[[:space:]]*(Cursor|Claude)' "$MSG_FILE" > "$tmp"
mv "$tmp" "$MSG_FILE"
